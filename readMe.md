# Chess project

## How to run?
"""
    Be in the root directory => Open a command prompt / Terminal => Run "docker-compose up --build"
"""

## How to test?
"""
    Be in the root directory => Open a command prompt / Terminal => Run "docker-compose up --build"
"""